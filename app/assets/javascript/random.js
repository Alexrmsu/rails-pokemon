// pequeña funcion para recargar el documento y generar nuevo pokemon aleatorio
const reload = document.getElementById('reload');

reload.addEventListener('click', _ => {
    location.reload();
});

